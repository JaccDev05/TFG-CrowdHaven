import { CommunityVisibility } from './community-visibility.enum';

describe('CommunityVisibility', () => {
  it('should create an instance', () => {
    expect(new CommunityVisibility()).toBeTruthy();
  });
});
